run.config:
  engine: nodejs
  extra_steps:
    - yarn install
  engine.config:
    runtime: nodejs-8.9
    dep_manager: yarn
    python_version: python-2.7
  extra_packages:
    - png

web.config:
  start: yarn server